#delcared by asking user to input int() wrapped statement-->converts user input type from string to integer then used to assess if-else statement
temperature = int(input("What is the temperature outside"))

if temperature > 80:
    print("turn on the AC.")
Else:
    print("Open the windows.")